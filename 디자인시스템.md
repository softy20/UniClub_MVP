# UniClub 디자인 시스템 (v1 홈 기준)

지금 구현된 **임원 첫 화면**을 기준으로 정리한 문서입니다. 새 화면을 만들 때 이 규칙을 먼저 보고, 토큰은 `src/index.css`만 바꿉니다.

레이아웃 원본: `디자인 틀2.png`  
구현: `src/App.tsx`, `src/components/`  
토큰: `src/index.css`

---

## 1. 원칙

- 랜딩 페이지가 아니라 **임원 운영 보드**.
- 장식(유리, 그라데이션, 차트, 큰 그림자) 없음.
- 색은 상태(필수, 지연, 임박)에만 쓴다. 배경을 테마색으로 물리지 않는다.
- 동아리 종목 비주얼(바다, 다이빙)을 크롬에 넣지 않는다.
- PC 우선. 폰에서는 같은 블록을 위에서 아래로 쌓는다.

---

## 2. 토큰

정의 위치: `src/index.css` `@theme`. Tailwind 클래스 `bg-canvas`, `text-ink` 등으로 연결됩니다.

### 색

| 토큰 | 현재 값 | 역할 |
|---|---|---|
| `canvas` | `#d9f7fa` | 페이지 배경, 홈 메뉴 선택, 선택 뱃지 |
| `surface` | `#ffffff` | 사이드바, 카드, 목록 패널 |
| `detail` | `#f0f8ff` | 할 일 상세 박스 |
| `ink` | `#2f3437` | 본문, 제목, 아이콘 |
| `muted` | `#787774` | 부가 정보, 비활성 메뉴 |
| `line` | `#eaeaea` | 1px 구분선·카드 테두리 |
| `red-bg` / `red-fg` | `#FDEBEC` / `#9F2F2D` | 필수 뱃지, 지연 D-Day |
| `yellow-bg` / `yellow-fg` | `#FBF3DB` / `#956400` | 오늘·임박 D-Day (`yellow-bg`는 아직 미사용) |
| `green-bg` / `green-fg` | `#EDF3EC` / `#346538` | 예약. 홈에서는 미사용 |

선택 영역(`::selection`): 배경 `#2f3437`, 글자 `#F7F6F3`.  
포커스 링: `2px solid #2f3437`, offset `2px`.

**주의 — `canvas`가 너무 많은 곳에 쓰임**

| 사용처 | 파일 |
|---|---|
| 페이지 배경 | `App.tsx` `bg-canvas`, `body` |
| 홈 메뉴 선택 | `Sidebar.tsx` `bg-canvas` |
| 선택 뱃지 | `TaskList.tsx` |
| 상세 박스 | `TaskList.tsx` `bg-detail` (`#f0f8ff`) |

`canvas` 값을 바꾸면 위가 한꺼번에 바뀝니다. 설계 때 분리할 토큰:

- `page` — 화면 뒤 배경
- `nav-active` — 선택된 메뉴
- `detail` — 이미 분리됨 (`#f0f8ff`)

### 타이포

- 본문 폰트: Pretendard. 대체 `Apple SD Gothic Neo`, `Noto Sans KR`.
- 숫자(D-Day): `.tabular` (고정폭 숫자).
- 자간: 제목 `-0.02em`, D-Day 큰 숫자 `-0.04em`, 뱃지 `0.05em`.
- 행간: 본문 `1.5`, 제목 `leading-snug`, 상세 `leading-relaxed`.

| 크기 | 굵기 | 쓰는 곳 |
|---|---|---|
| 56px | semibold | 행사 D-Day 숫자 |
| 20px | semibold | 행사 이름 |
| 16px | semibold | 패널 제목 (할 일 목록, 다음주 할 일) |
| 15px | semibold | 로고 UniClub |
| 14px | medium / regular | 메뉴, 할 일 제목 |
| 13px | regular | 클럽명, 상세 본문, 빈 상태 |
| 12px | regular / medium | 부가 줄, 업무 D-Day |
| 10px | medium | 필수/선택 뱃지 |

### 간격 · 모서리 · 깊이

- 페이지 패딩: `16px` (모바일) / `20px` (PC).
- 카드 안: 헤더 `20×16`, D-Day 카드 `24`, 할 일 행 세로 `14`.
- 모서리: 카드 `12px`, 상세 박스 `8px`, 메뉴·버튼 `6px`, 뱃지 알약 `9999px`.
- 그림자 없음. 경계는 `1px solid line`만.
- 아이콘: Phosphor **Bold**. 메뉴 18px, 체크 22px, 펼침 화살표 14px.

---

## 3. 레이아웃 (임원 홈)

와이어프레임 `디자인 틀2.png`와 같습니다.

```
┌────────┬────────────┬─────────────────┐
│ 로고   │ 행사 D-Day │ 할 일 목록      │
│ 메뉴   │            │                 │
│        ├────────────┴─────────────────┤
│        │ 다음주 할 일                 │
└────────┴──────────────────────────────┘
```

PC (`lg` 이상, 약 1024px+):

```
grid-cols: 232px | minmax(280px, 340px) | 1fr
grid-rows: minmax(280px, auto) | 1fr
```

- 사이드바: 두 행을 세로로 차지.
- 다음주 할 일: 메인 영역 전체 너비 (`lg:col-span-2`).

모바일: 사이드바 → D-Day → 할 일 → 다음주 순으로 한 열.

메뉴 항목: 홈(현재) / 연간 일정 / 회계. 홈만 동작. 나머지는 자리만.

---

## 4. 컴포넌트

### 사이드바 `Sidebar.tsx`

- 표면: `surface`. 오른쪽(PC) 또는 아래(모바일) `line` 보더.
- 로고: UniClub (`ink`) + `{동아리명} · {학년도}` (`muted`).
- 선택 메뉴: `canvas` 배경, `ink` 글자.
- 비선택: 배경 없음, `muted`. 아직 링크 아님.

### 행사 D-Day `EventDday.tsx`

- **행사** 카운트다운. 업무별 D-Day와 다름.
- 제일 가까운 미래 행사 1개. 기준일 `2026-09-02` (`src/lib/board.ts` `TODAY`).
- 구성: 행사명(제목) → 날짜 → (장소) → 큰 `D-n` 또는 `오늘`.
- 카드: `surface`, 모서리 12px, 패딩 24px.

### 할 일 패널

할 일 목록 / 다음주 할 일이 같은 카드 패턴.

- `surface` + `line` + 모서리 12px.
- 헤더: 제목 왼쪽, 날짜 범위 오른쪽(`muted` 12px).
- 빈 상태: `muted` 14px, 패딩 32px.

### 할 일 행 `TaskList.tsx`

한 행은 역할이 **셋**으로 갈립니다. 한 칸에 체크와 상세를 같이 넣지 않습니다.

| 영역 | 동작 | 크기 |
|---|---|---|
| 왼쪽 체크 | 완료 토글. `localStorage` | 너비 44px (`w-11`) |
| 가운데 제목 | 상세 펼침/접힘 | 남은 너비 |
| 오른쪽 | 표시만. 업무 D-Day + 필수/선택 | 우측 패딩 16px |

업무 D-Day 글자색:

- 지연 → `red-fg` (`지연 n일`)
- 오늘·3일 이내 → `yellow-fg`
- 그 외 → `muted` (`D-n`)

뱃지: 필수 = `red-bg`/`red-fg`. 선택 = `canvas`/`muted`.

완료된 제목: `muted` + 취소선.

### 상세 박스

제목을 누르면 행 **아래**에 펼칩니다. 모달 아님.

- 현재: `mx-4 mb-3.5`, 모서리 8px, `border-line`, **`bg-detail` (`#f0f8ff`)**.
- 본문 13px `ink`. 체크리스트 13px `muted`.
- 상세가 없으면: `이 할 일에 적힌 상세 수칙이 없습니다.`
- 페이지 배경색을 여기서 쓰지 않는 것이 다음 설계 과제.

---

## 5. 움직임 · 상태

- 펼침 화살표만 `150ms` 회전. 입장 애니메이션 없음.
- 체크: 기본 / 완료(`aria-pressed`).
- 상세: 접힘 / 펼침(`aria-expanded`).
- 포커스: 키보드 링 2px `ink`.
- 호버 스타일은 아직 없음.

---

## 6. 하지 말 것

- Inter, Lucide, 그라데이션, 글래스, 큰 `shadow`.
- 이모지로 아이콘 대체.
- KPI 숫자 카드, 차트, 지도.
- 같은 크기 아이콘 타일 런처.
- 스쿠버/바다 테마를 UI 크롬에 고정.
- 상세 박스에 페이지 테마색을 그대로 칠하기.

---

## 7. 다음 화면을 그릴 때

1. 새 블록도 `surface` 카드 + `line` 테두리 + 12px 모서리.
2. 색이 필요하면 `red` / `yellow` 상태만. 새 배경색이 필요하면 **토큰을 새로 만들고** `canvas`를 재사용하지 않는다.
3. 할 일·행사·회계는 이 홈의 타이포 스케일을 그대로 쓴다.
4. PC에서 사이드바 232px은 유지하는 것을 기본으로 한다.
