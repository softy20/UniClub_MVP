# UniClub 디자인 시스템 (Figma Make)

Figma Make [Team To-Do List Calendar](https://www.figma.com/make/Xi6TEXLgv32CVmCskPG9MA/Team-To-Do-List-Calendar)를 기준으로 정리한 문서입니다. 예전 청록 캔버스 홈(`디자인 틀2.png`)은 더 이상 레이아웃 원본이 아닙니다.

구현: `src/App.tsx`, `src/components/Sidebar.tsx`, `src/components/DashboardPage.tsx`, `src/components/MakeCalendar.tsx`, `src/components/TasksPage.tsx`, `src/components/AiParsePage.tsx`, `src/components/EventPanel.tsx`  
토큰: `src/index.css`  
데이터: `src/data/club.json` → `src/lib/ops.ts`  
시계: `src/lib/kst.ts` (`Asia/Seoul`)

---

## 1. 원칙

- 임원 운영 보드. 랜딩 페이지가 아님.
- 메뉴는 **대시보드 / 캘린더 / 할 일 목록 / AI 파싱** 네 개. 회계는 메뉴에 없음.
- 앱 진입은 대시보드.
- 본문은 Pretendard Variable. 숫자·큰 제목은 Outfit (`font-display`).
- Primary는 파랑 `#0066FF`. 선택 메뉴는 인디고 틴트.
- 카테고리 색 7종만 행사 종류 식별에 쓴다.
- 동아리 종목 비주얼을 크롬에 넣지 않는다. 범례 이름(OW, 해양 등)은 데이터다.
- PC 우선. 셸은 `h-screen` 좌측 사이드바 + 우측 메인.

---

## 2. 토큰

정의 위치: `src/index.css` `@theme`와 `:root`.

### 색

| 토큰 | 값 | 역할 |
|---|---|---|
| `bg` | `#f7f7f9` | 페이지·캘린더 뒤 배경 |
| `bg2` | `#ffffff` | 상단 헤더 |
| `card` | `#ffffff` | 카드, 달력 상자, 행 |
| `card2` | `#f2f2f5` | 표 헤더, 체크 행, 빈 칸 |
| `border` / `border2` | `#e4e4ea` / `#d0d0d8` | 1px 구분선 |
| `fg` / `fg2` / `fg3` | `#111118` / `#4a4a5a` / `#9090a0` | 본문 / 보조 / 캡션 |
| `accent` / `accent2` | `#0066FF` / `#0052CC` | 오늘 점, 버튼, 활성 메뉴 글자 |
| `sidebar` | `#f0f0f5` | 왼쪽 레일 |

로고 마크: `linear-gradient(135deg, #6366f1, #a855f7)`.  
선택 메뉴: 배경 `rgba(99,102,241,0.15)`, 테두리 `rgba(99,102,241,0.3)`.  
포커스 링: `2px solid #0066FF`.  
선택 영역: 배경 `#111118`, 글자 `#F7F7F8`.

### 카테고리 7종

| 번호 | 라벨 | 점·글자 | 칩 배경 |
|---|---|---|---|
| 1 | OW | `#3b82f6` | `rgba(59,130,246,0.15)` |
| 2 | AOW | `#E8492C` | `rgba(232,73,44,0.15)` |
| 3 | 해양 | `#00838F` | `rgba(0,131,143,0.15)` |
| 4 | 회식 | `#5BA8D4` | `rgba(91,168,212,0.15)` |
| 5 | 회의 | `#6A4FE0` | `rgba(106,79,224,0.15)` |
| 6 | 봉사 | `#8E24AA` | `rgba(142,36,170,0.15)` |
| 7 | MT | `#D81B7A` | `rgba(216,27,122,0.15)` |

`club.json` 범례(OW·AOW·해양·대면·임원·부스·MT)는 `src/lib/ops.ts`에서 위 7종으로 대응한다. 부스→봉사, 대면→회식, 임원→회의.

### 타이포

- 본문: Pretendard Variable.
- 디스플레이: Outfit. 대시보드 숫자, 행사명, 메뉴.
- 캡션·칩: 12–13px, tracking-widest 라벨은 대문자.

| 크기 | 굵기 | 쓰는 곳 |
|---|---|---|
| 22px | bold | 캘린더 월 제목 |
| 24px | bold | 대시보드 KPI 숫자 |
| 18px | bold | 페이지 제목, 패널 행사명 |
| 14px | medium | 메뉴, 할 일 제목 |
| 13px | medium / semibold | 칩, 오늘 날짜, 진행률 보조 |
| 12px | regular | 섹션 라벨, 부가 줄 |
| 11px | regular | 달력 칸 행사명 |

### 간격 · 모서리 · 깊이

- 메인 패딩: `24px` (`p-6`).
- 사이드바 너비: `224px` (`w-56`).
- 행사 패널: `400px`, 오른쪽 고정, 뒤 딤 `bg-black/50`.
- 카드 모서리: `12px` (`rounded-xl`). 칩 `10px`. 메뉴 `8px`. 로고 `8px`.
- 달력 칸 높이: `110px`.
- 아이콘: Phosphor Bold 18px (메뉴), 닫기 16px.

---

## 3. 셸

```
┌──────────┬────────────────────────────────┐
│ UniClub  │  페이지 제목 · 오늘 날짜        │
│ 진행률   ├────────────────────────────────┤
│ 메뉴 4개 │  대시보드 / 캘린더 / 할 일 /   │
│ 카테고리 │  AI 파싱                         │
│ 클럽명   │                                │
└──────────┴────────────────────────────────┘
```

### 사이드바 `Sidebar.tsx`

- 배경 `sidebar`, 오른쪽 `border`.
- 로고 U 마크 + UniClub + `MVP v0.1`.
- 2학기 전체 진행률 바 (완료 태스크 / 전체 태스크).
- 메뉴: 대시보드, 캘린더, 할 일 목록, AI 파싱. 회계 없음.
- 활성 메뉴: 인디고 틴트 + `accent2` 글자.
- 카테고리 목록: 점 + 이름 + 해당 행사 수. 0개인 항목은 숨김.
- 하단: 동아리명 이니셜 원 + `임원 보드`. 가상 인물 이름은 넣지 않음.

### 헤더

- 왼쪽: 현재 메뉴 이름 + `{학년도}년 · {동아리명}`.
- 오른쪽: 초록 점 + `오늘 N월 N일` (KST).

---

## 4. 대시보드

KPI 4장: 전체 행사, 완료 태스크, D-7 이내, 임원 수(`roles` 길이).  
그 아래 D-7 긴급 행사 카드. 없으면 `D-7 이내 긴급 행사 없음`.  
맨 아래 전체 행사 표(이름, 날짜, 카테고리, D-Day, 진행률). 행 클릭 → 행사 패널.

D-Day 점: D+ 회색, 1일 이내 빨강, 7일 이내 노랑, 그 외 초록.

---

## 5. 캘린더

월 ‹ › + 필터 칩(전체 + 7종). 칩은 여러 개 동시에 켤 수 있음. 전부 끄면 행사 숨김.  
한 상자 안에 요일 헤더 + 날짜 격자. 내부 1px `border`.  
오늘: 파란 원 + 흰 숫자, 칸 배경 `rgba(0,102,255,0.04)`.  
일요일 빨강, 토요일 파랑.  
칸 안 행사는 카테고리 칩 최대 3개. 넘으면 `+n`. 클릭 → 행사 패널.

---

## 6. 할 일 목록

행사 태스크를 한 목록으로 펼침. `localStorage` (`uniclub-done-v1`)로 체크.  
기본은 미완료만. `완료 보기`로 완료 포함.  
카테고리 필터는 캘린더와 같음.  
행: 체크 + 제목 + 태그 + 행사명 + `D-Day N일 전`.

---

## 7. AI 파싱

업로드 드롭존(`.md` `.docx` `.pdf`) + 샘플 파싱 미리보기.  
샘플 시작 → 진행 단계 5개 → 결과 JSON 미리보기.  
실제 Claude ping과는 아직 연결하지 않음. UI 껍데기.

---

## 8. 행사 패널

오른쪽 슬라이드. 딤 클릭 또는 X로 닫힘.  
헤더(태그, D-Day, 제목, 날짜·장소) → 진행률 바 → 체크리스트 / 메모 탭.

---

## 9. 움직임

- 페이지: `fade-in` 200ms.
- 패널: `slide-in` 250ms.
- 오늘 점·긴급 점: `pulse-dot` 2s.
- 대시보드 긴급 카드 호버: `scale-[1.01]`.

---

## 10. 하지 말 것

- 회계 메뉴를 다시 넣지 말 것. 회계 작업은 나중.
- 사이드바에 가상 인물 이름(예: 김민준)을 넣지 말 것.
- 부원 수를 데이터에 없이 지어내지 말 것. KPI는 임원 역할 수.
- 예전 `canvas` `#d9f7fa` 홈 그리드(D-Day 카드 + 할 일 + 다음주)로 되돌리지 말 것.
- 범례를 달력 상자 밖에 따로 두지 말 것. 필터 칩이 범례다.
- Inter를 본문으로 쓰지 말 것. Pretendard + Outfit.
