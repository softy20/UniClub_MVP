# UniClub 디자인 시스템 (v1 홈 · 연간 일정)

**임원 홈**과 **연간 일정 월 달력**이 구현된 상태를 기준으로 정리한 문서입니다. 새 화면을 만들 때 이 규칙을 먼저 보고, 토큰은 `src/index.css`만 바꿉니다.

레이아웃 원본: `concept/디자인 틀2.png` (홈), 범례 동작 참고 `concept/캘린더 움짤(범례).gif`  
구현: `src/App.tsx`, `src/components/YearCalendar.tsx`, `src/components/Sidebar.tsx`  
시계: `src/lib/kst.ts` (`Asia/Seoul`)  
토큰: `src/index.css`

---

## 1. 원칙

- 랜딩 페이지가 아니라 **임원 운영 보드**.
- 장식(유리, 그라데이션, 차트, 큰 그림자) 없음.
- 홈에서 색은 상태(필수, 지연, 임박)에만 쓴다. 배경을 테마색으로 물리지 않는다.
- 달력 점·범례만 행사 **종류** 식별에 색을 쓴다. 메뉴·카드 배경·선택 원에는 쓰지 않는다. 예외: 오늘 날짜 테두리 `#2563EB`, 호버 배경 `#e6f5f5`.
- 동아리 종목 비주얼(바다, 다이빙)을 크롬에 넣지 않는다. 범례 이름(OW, 해양 등)은 데이터지 UI 테마가 아니다.
- PC 우선. 폰에서는 같은 블록을 위에서 아래로 쌓는다.

---

## 2. 토큰

정의 위치: `src/index.css` `@theme`. Tailwind 클래스 `bg-canvas`, `text-ink` 등으로 연결됩니다.

### 색

| 토큰 | 현재 값 | 역할 |
|---|---|---|
| `canvas` | `#d9f7fa` | 페이지 배경, 홈 메뉴 선택, 선택 뱃지 |
| `surface` | `#ffffff` | 사이드바, 카드, 목록 패널 |
| `detail` | `#f0f8ff` | 할 일 상세 박스 |
| `ink` | `#2f3437` | 본문, 제목, 아이콘 |
| `muted` | `#787774` | 부가 정보, 비활성 메뉴 |
| `line` | `#eaeaea` | 1px 구분선·카드 테두리 |
| `red-bg` / `red-fg` | `#FDEBEC` / `#9F2F2D` | 필수 뱃지, 지연 D-Day |
| `yellow-bg` / `yellow-fg` | `#FBF3DB` / `#956400` | 오늘·임박 D-Day (`yellow-bg`는 아직 미사용) |
| `green-bg` / `green-fg` | `#EDF3EC` / `#346538` | 예약. 홈에서는 미사용 |

선택 영역(`::selection`): 배경 `#2f3437`, 글자 `#F7F6F3`.  
포커스 링: `2px solid #2f3437`, offset `2px`.

**주의 — `canvas`가 너무 많은 곳에 쓰임**

| 사용처 | 파일 |
|---|---|
| 페이지 배경 | `App.tsx` `bg-canvas`, `body` |
| 홈 메뉴 선택 | `Sidebar.tsx` `bg-canvas` |
| 선택 뱃지 | `TaskList.tsx` |
| 상세 박스 | `TaskList.tsx` `bg-detail` (`#f0f8ff`) |

`canvas` 값을 바꾸면 위가 한꺼번에 바뀝니다. 설계 때 분리할 토큰:

- `page` — 화면 뒤 배경
- `nav-active` — 선택된 메뉴
- `detail` — 이미 분리됨 (`#f0f8ff`)

호버 배경 `#e6f5f5`, 오늘 테두리 `#2563EB`는 `@theme`에 아직 없음. 클래스에서 직접 쓴다.

### 달력 전용 (`src/index.css` `@theme`)

선택 원: `cal-selected` `#e8e8e8`. `canvas`(청록)나 움짤의 초록/하늘 네모가 아님.

범례 점 — 오션홀릭 데모 7종. 다른 동아리면 데이터만 바뀐다. 크롬에 하드코딩하지 않는다.

| 범례 | 점 색 | 넣는 행사 |
|---|---|---|
| OW | `#4A90C8` | 이론, 스킨, OW 회차, 수영장 정기 훈련, 체크 다이빙 |
| AOW | `#E07A5F` | AOW 회차 |
| 해양 | `#1D4E89` | 하계 양양, 가을 선택 다이빙, 제주·해외 투어 |
| 대면 | `#E879A9` | OT·개강 회식, 번개, 가을 레저, 잠수인의 밤, 송년회 |
| 임원 | `#65A30D` | 모집·면접, 재등록, 회의, 임원 선출, 추석·국시 선물 |
| 부스 | `#EA580C` | 드림데이즈, 축제·지역 부스 |
| MT | `#8B5CF6` | 하계 빠지 MT, 동계 스키 MT |

홈의 `red-fg`(필수/지연)와 AOW 점을 같은 빨강으로 쓰지 않는다. OW는 `canvas`보다 진해야 점이 보인다.

### 타이포

- 본문 폰트: Pretendard. 대체 `Apple SD Gothic Neo`, `Noto Sans KR`.
- 숫자(D-Day): `.tabular` (고정폭 숫자).
- 자간: 제목 `-0.02em`, D-Day 큰 숫자 `-0.04em`, 뱃지 `0.05em`.
- 행간: 본문 `1.5`, 제목 `leading-snug`, 상세 `leading-relaxed`.

| 크기 | 굵기 | 쓰는 곳 |
|---|---|---|
| 56px | semibold | 행사 D-Day 숫자 |
| 20px | semibold | 행사 이름 |
| 16px | semibold | 패널 제목 (할 일 목록, 다음주 할 일, 월 달력 헤더) |
| 15px | semibold | 로고 UniClub |
| 14px | medium / regular | 메뉴, 할 일 제목 |
| 13px | regular | 클럽명, 상세 본문, 빈 상태 |
| 12px | regular / medium | 부가 줄, 업무 D-Day, KST 시계 |
| 10px | medium | 필수/선택 뱃지 |

### 간격 · 모서리 · 깊이

- 페이지 패딩: `16px` (모바일) / `20px` (PC).
- 카드 안: 헤더 `20×16`, D-Day 카드 `24`, 할 일 행 세로 `14`.
- 모서리: 카드 `12px`, 상세 박스 `8px`, 메뉴·버튼 `6px`, 뱃지 알약 `9999px`.
- 그림자 없음. 경계는 `1px solid line`만.
- 아이콘: Phosphor **Bold**. 메뉴 18px, 체크 22px, 펼침 화살표 14px.

---

## 3. 레이아웃 (임원 홈)

와이어프레임 `디자인 틀2.png`와 같습니다.

```
┌────────┬────────────┬─────────────────┐
│ 로고   │ 행사 D-Day │ 할 일 목록      │
│ 메뉴   │            │                 │
│        ├────────────┴─────────────────┤
│        │ 다음주 할 일                 │
└────────┴──────────────────────────────┘
```

PC (`lg` 이상, 약 1024px+):

```
grid-cols: 232px | minmax(280px, 340px) | 1fr
grid-rows: minmax(280px, auto) | 1fr
```

- 사이드바: 두 행을 세로로 차지.
- 다음주 할 일: 메인 영역 전체 너비 (`lg:col-span-2`).

모바일: 사이드바 → D-Day → 할 일 → 다음주 순으로 한 열.

메뉴 항목: 홈 / 연간 일정 / 회계. 홈과 연간 일정 동작. 앱 진입은 연간 일정. 회계는 자리만.

---

## 4. 컴포넌트

### 사이드바 `Sidebar.tsx`

- 표면: `surface`. 오른쪽(PC) 또는 아래(모바일) `line` 보더.
- 로고: UniClub (`ink`) + `{동아리명} · {학년도}` (`muted`).
- 선택 메뉴: `canvas` 배경, `ink` 글자.
- 비선택: 배경 없음, `muted`. 홈·연간 일정은 링크. 회계는 자리만(`cursor-default`).
- 연간 일정 화면만 `rail`. PC(`lg`+) 232px 고정. `lg` 미만은 아이콘 레일 56px(`w-14`). 햄버거(三) 클릭으로 고정, 호버로 본문 위 레이어 232px 확장(`200ms` 너비). PC에서는 햄버거 없음.

### 행사 D-Day `EventDday.tsx`

- **행사** 카운트다운. 업무별 D-Day와 다름.
- 제일 가까운 미래 행사 1개. 기준일은 한국 시간(`Asia/Seoul`, UTC+9)의 **실제 오늘**.
- 구성: 행사명(제목) → 날짜 → (장소) → 큰 `D-n` 또는 `오늘`.
- 카드: `surface`, 모서리 12px, 패딩 24px.

### 할 일 패널

할 일 목록 / 다음주 할 일이 같은 카드 패턴.

- `surface` + `line` + 모서리 12px.
- 헤더: 제목 왼쪽, 오른쪽은 KST 시계 + 주 범위(`muted` 12px). 시계 형식은 6절과 같음.
- 빈 상태: `muted` 14px, 패딩 32px.

### 할 일 행 `TaskList.tsx`

한 행은 역할이 **셋**으로 갈립니다. 한 칸에 체크와 상세를 같이 넣지 않습니다.

| 영역 | 동작 | 크기 |
|---|---|---|
| 왼쪽 체크 | 완료 토글. `localStorage` | 너비 44px (`w-11`) |
| 가운데 제목 | 상세 펼침/접힘 | 남은 너비 |
| 오른쪽 | 표시만. 업무 D-Day + 필수/선택 | 우측 패딩 16px |

업무 D-Day 글자색:

- 지연 → `red-fg` (`지연 n일`)
- 오늘·3일 이내 → `yellow-fg`
- 그 외 → `muted` (`D-n`)

뱃지: 필수 = `red-bg`/`red-fg`. 선택 = `canvas`/`muted`.

완료된 제목: `muted` + 취소선.

### 상세 박스

제목을 누르면 행 **아래**에 펼칩니다. 모달 아님.

- 현재: `mx-4 mb-3.5`, 모서리 8px, `border-line`, **`bg-detail` (`#f0f8ff`)**.
- 본문 13px `ink`. 체크리스트 13px `muted`.
- 상세가 없으면: `이 할 일에 적힌 상세 수칙이 없습니다.`
- 페이지 배경색을 여기서 쓰지 않는 것이 다음 설계 과제.

---

## 5. 움직임 · 상태 (홈)

달력 패널은 6절.

- 펼침 화살표만 `150ms` 회전. 입장 애니메이션 없음.
- 체크: 기본 / 완료(`aria-pressed`).
- 상세: 접힘 / 펼침(`aria-expanded`).
- 포커스: 키보드 링 2px `ink`.
- 메뉴 호버: `#e6f5f5`, `cursor-pointer`, `150ms`. 할 일 행 호버는 없음.

---

## 6. 연간 일정 (월 달력)

구현: `YearCalendar.tsx`. 홈 그리드(D-Day + 할 일)를 쓰지 않는다. 시계·D-Day 기준일은 KST(`Asia/Seoul`).

참고: `concept/캘린더 움짤(범례).gif` — 범례 토글 동작만 가져온다. 선택 표시의 초록/하늘 네모는 쓰지 않는다.

### 처음: 월 달력만

```
┌──────────┬─────────────────────────────────────────────┐
│ 메뉴     │  [ 월 달력 카드 ]  max 700px, 가운데         │
│ (rail)   │                                             │
│          │  [ 범례 카드 ]                               │
│          │  항목 선택 시 해당 항목만,                   │
│          │  다시 선택하면 전체 항목이 표시됩니다.        │
│          │  ●OW  ●AOW  ●해양  ●대면  ●임원  ●부스  ●MT │
└──────────┴─────────────────────────────────────────────┘
```

왼쪽 메뉴는 4절 `rail`. 메인은 달력 카드와 범례 카드를 **위·아래 다른 흰 상자**. 둘 다 `surface` + `line` + 모서리 12px. 두 카드는 같은 열 `max-w-[700px]` `mx-auto`.

달력 카드:

- 헤더: 월 제목(16px semibold, 클릭하면 연·월 선택) + KST 시계(12px `muted`) + 이전/다음 달. 숫자는 `.tabular`.
- 시계: `YYYY.MM.DD (요일) HH:mm`. 초 없음. 예: `2026.09.04 (금) 17:27`.
- 요일: 일~토. 날짜 숫자는 14px `ink`.
- 점은 날짜 숫자 **바로 아래**. 같은 날이면 **가로**로 여러 개.
- **오늘:** 숫자 칸에 `1.5px solid #2563EB` 원 테두리. 선택 원과 겹쳐도 테두리는 남긴다.
- 첫 화면의 달은 KST 오늘이 속한 달.

월 제목 클릭: 연도 ‹ › + 1~12월 격자. 보고 있는 달은 `cal-selected`. 바깥 클릭 또는 Esc로 닫힘. 월을 고르면 그달로 이동.

범례 카드 (달력과 다른 상자):

- 설명 한 줄, `muted` 13px: `항목 선택 시 해당 항목만, 다시 선택하면 전체 항목이 표시됩니다.`
- 그 아래 점+이름 7개. 날짜를 눌러 오른쪽 패널이 열려도 **범례는 달력 아래 그대로**.

### 범례 필터 (토글 하나)

| 조작 | 달력 점 | 범례 칩 |
|---|---|---|
| 처음 | 전부 | 선택 없음 |
| `해양` 한 번 | 남색만 | 그 항목만 연한 배경 (`line` 정도. `canvas` 아님) |
| 같은 `해양` 다시 | 전부 복귀 | 선택 해제 |
| 다른 항목 | 그 종류만 | 이전 선택 해제, 새 항목만 선택 |

필터가 켜진 날만 점이 남는다. 오른쪽 패널도 **보이는 종류만** 보여 준다. 빈 날(점 없음, 또는 가려져서 없음)을 눌러도 패널은 열리되, 가려진 일정은 안 나온다.

### 날짜 클릭: 연회색 원 + 오른쪽 패널

```
┌──────────┬──────────────────┬──────────────┐
│ 메뉴     │  달력·범례        │ 9월 12일 토  │
│          │  (왼쪽으로 밀림)  │        [X]   │
│          │  max 700px       │ 행사 이름    │
│          │                  │ 상세 · 사진  │
└──────────┴──────────────────┴──────────────┘
```

- **선택:** 날짜 숫자에 연회색 **원** (`cal-selected`). 점 아래는 그대로. 움짤의 초록/하늘 네모 없음.
- **패널 날짜:** 올해 `9월 12일 토`. 다른 해 `2027년 9월 12일 토`.
- **패널:** 흐름 안 오른쪽. 너비 `0` → `340px`, `200ms`. 딤·fixed 오버레이 없음. 달력 열(`flex-1`)이 줄어 700px 카드가 왼쪽으로 밀린다. 왼쪽 메뉴는 그대로.
- **닫기:** 패널 오른쪽 위 **X만**. 같은 날짜를 다시 눌러도 닫히지 않음. 다른 날을 누르면 그날로 바꿈.
- **패널 내용 순서:** 날짜 → 일정 이름(20px semibold) → 일정 상세(13px `ink`) → 행사 사진.
- 같은 날에 보이는 행사가 여러 개면 그 순서로 쌓는다.
- **사진:** 이 패널에서만 첨부·표시. 갤러리 페이지는 나중에.
- 빈 날: 패널은 열고 `이 날 등록된 행사가 없습니다`. 사진 영역 없음.

패널 빈 상태 문구는 할 일 상세와 같이 `muted` 13px.

### 움직임 · 호버

- 오른쪽 패널 너비 `200ms`. 달력 입장 애니메이션 없음.
- 범례 칩: 기본 / 선택(`aria-pressed`). 한 번에 하나만.
- 날짜 칸: 선택(`aria-selected`).
- 클릭 가능 요소(날짜 칸, 범례, 월·연 화살표, 월 피커, X, 사진 첨부, 왼쪽 메뉴): `cursor-pointer`, 호버 배경 `#e6f5f5`, `transition-colors duration-150`. 호버는 칸 배경만. 선택 원·오늘 테두리는 숫자 `span`에 둔다.
- 포커스: 키보드 링 2px `ink`.

---

## 7. 하지 말 것

- Inter, Lucide, 그라데이션, 글래스, 큰 `shadow`.
- 이모지로 아이콘 대체.
- KPI 숫자 카드, 차트, 지도.
- 같은 크기 아이콘 타일 런처.
- 스쿠버/바다 테마를 UI 크롬에 고정.
- 상세 박스에 페이지 테마색을 그대로 칠하기.
- 달력 선택을 초록/하늘 **네모**로 표시.
- 범례를 달력 카드 **안에** 넣기.
- 범례를 여러 개 동시에 켜기.
- 선택 원·범례 칩 배경에 `canvas`를 쓰기.
- 일정 패널을 딤(Backdrop) + fixed 오버레이로 띄우기.
- 패널을 날짜 재클릭·바깥 클릭으로 닫기. 닫기는 X만.

---

## 8. 다음 화면을 그릴 때

1. 새 블록도 `surface` 카드 + `line` 테두리 + 12px 모서리.
2. 홈 상태색은 `red` / `yellow`만. 달력 종류색은 위 7종 토큰. 새 배경이 필요하면 **토큰을 새로 만들고** `canvas`를 재사용하지 않는다.
3. 할 일·행사·회계·달력 패널은 이 홈의 타이포 스케일을 그대로 쓴다.
4. PC에서 사이드바 232px은 유지. 연간 일정 `lg` 미만은 레일 56px. 오른쪽 일정 패널은 이 메뉴와 별개다.
5. 회계는 아직 시안 없음.
